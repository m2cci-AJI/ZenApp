import { Route } from '@angular/router';
import { DurationSessionComponent } from './duration-session.component';

export const durationSessionRoute: Route = {
  path: 'duration-session',
  component: DurationSessionComponent
};
