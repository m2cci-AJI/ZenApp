export class Comment {
    sensations: string;
    emotions: string;
    pensees: string;
    constructor(sensations: string, emotions: string, pensees: string) {
       this.sensations = sensations;
       this.emotions = emotions;
       this.pensees = pensees;
    }
  }
